TODO
====

	- validate GridViewControls
	- GridView ajaxupdate - update paganation, update all sort columns? (update filters, then set focus?)

	[Due in 6.6]
	- Column ctors should be identical (requires breaking backwards compatability)
		- ActiveRecord column mapping
		- ActiveRecord filter mapping??

	[Features]
	- combine css/js theme files into a single file
	- migration tests
	- User Preferences

PLUGINS
=======

Components (BOD)
==========

CONTROLS (BOD)
========

BUGS/ISSUES
===========

	RequiredValidator does not work with ListBox when multiple=true
	ENT_QUOTES causes GridViewControls to fail epically
	DateSelector AJAX fails epically
